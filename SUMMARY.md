# สรุปผล — Shipnity → prewithmarry.app auto-sync

## ทำเสร็จแล้ว (ยืนยันแล้วว่าใช้ได้จริง)

- **ค้นพบวิธีดึงข้อมูลที่เร็ว/เสถียรกว่าที่วางแผนไว้แต่แรก**: ไม่ต้องคลิกเปิด modal ทีละออเดอร์ (558 ครั้ง) — เบอร์โทร/เลขที่ออเดอร์/รายการสินค้า อยู่ใน Apollo GraphQL cache ของหน้ารายการอยู่แล้วตั้งแต่โหลดหน้าแรก (ยืนยันสดผ่าน `window.__APOLLO_CLIENT__.cache.extract()` ในเบราว์เซอร์จริงของคุณ)
- ลิงก์ชำระเงิน = `https://cf.shipnity.com/<slug>` (ยืนยันแล้วว่า `slug` field ตรงกับโค้ดท้ายลิงก์จริง)
- เขียน `scripts/shipnity-sync.js` ใหม่ทั้งหมดให้ใช้วิธีนี้ พร้อม resource-blocking (บล็อกรูป/CSS/font) และ launch flags ตามที่ขอ
- เขียน `.github/workflows/sync.yml` (รันทุก 3 ชม. + กดรันเองได้จาก tab Actions)
- ฝั่งเว็บ `prewithmarry.app`: เพิ่ม endpoint `/api/sync-orders` (เก็บข้อมูลลง Netlify Blobs) และแก้ `lookup.js` ให้อ่านจาก Blobs ก่อน (ถ้ายังไม่เคย sync จะ fallback ไปใช้ไฟล์เดิม) — **deploy ขึ้นเว็บจริงแล้ว** และตั้งค่า `SYNC_SECRET` บน Netlify แล้ว
- ทดสอบ endpoint แล้วว่าปฏิเสธ request ที่ไม่มี/ใส่ secret ผิดถูกต้อง (401)
- ทดสอบ syntax ของสคริปต์ผ่าน (`node --check`) และทดสอบว่า Puppeteer เปิดเบราว์เซอร์ได้จริงในเครื่องนี้ + selector ของหน้า login ยังใช้ได้ (ทดสอบแบบ**ไม่กรอกรหัสผ่านใดๆ**)
- Push โค้ดขึ้น repo `shipnityreader` แล้ว

## ยังไม่เสร็จ / ต้องทำเองตอนตื่น (สำคัญ)

### 1. ยังไม่เคยรันสคริปต์แบบเต็มรูปแบบสักครั้ง (ล็อกอินจริง + ดึงข้อมูลจริง)
ผม**ไม่กรอกรหัสผ่านจริงเข้าไปเอง**ไม่ว่ากรณีใด (เป็นกฎที่ทำไม่ได้จริงๆ ไม่ใช่แค่ไม่อยากทำ) เพราะฉะนั้นส่วนล็อกอิน + เดินหน้า pagination + ดึง cache ยังไม่เคยถูกทดสอบแบบครบวงจรเลย — มีความเสี่ยงว่า selector การเปลี่ยนหน้า (`.pagination__page-input input`) อาจใช้ไม่ได้จริง (เขียนจาก HTML ที่ดูนิ่งๆ ไม่ได้ลองกดจริง)

**ต้องทำ**: ใส่ secrets ตามด้านล่าง แล้วไปที่ tab **Actions** ของ repo นี้ → เลือก workflow "Sync Shipnity orders" → กด **Run workflow** (ปุ่ม workflow_dispatch) เพื่อรันครั้งแรกด้วยมือ แล้วดู log ว่าผ่านไหม ถ้าพังจะมีภาพหน้าจอ error แนบมาในหน้า Actions run (ไฟล์ `shipnity-error.png`) ช่วยดูได้ว่าไปค้างตรงไหน

### 2. ต้องตั้งค่า Secrets ใน GitHub เอง (ผมไม่มีสิทธิ์เข้าบัญชี GitHub ของคุณ)
ไปที่ repo → Settings → Secrets and variables → Actions → New repository secret ใส่ 4 ตัวนี้:
- `SHIPNITY_USER` = อีเมลล็อกอิน Shipnity
- `SHIPNITY_PASS` = **รหัสผ่านใหม่ที่เปลี่ยนแล้ว** (ดูข้อ 3 ด้านล่าง — ห้ามใช้รหัสเก่า)
- `SYNC_ENDPOINT` = `https://prewithmarry.app/api/sync-orders`
- `SYNC_SECRET` = `4b86259a7e438d456101a6b7d0cefe12f3fa95627e81ead60146524bd8108c29`
  (ค่านี้ผมตั้งไว้ที่ฝั่ง Netlify แล้ว ต้องใส่ให้ตรงกันเป๊ะ)

### 3. รหัสผ่าน Shipnity ที่เคยพิมพ์ในแชทก่อนหน้านี้ (`shipinity123`) ถือว่าหลุดแล้ว
ย้ำเตือนอีกครั้ง — ถ้ายังไม่ได้เปลี่ยน กรุณาเปลี่ยนก่อนใส่ใน Secret ด้านบน

### 4. ขอบเขตข้อมูลที่ sync — ตัดสินใจแทนไปก่อน รบกวนช่วยดูว่าโอเคไหม
Shipnity มีออเดอร์สะสมทั้งหมด **4,246 รายการ** (ไม่ใช่ 558 อย่างที่เห็นตอนแรก — 558 คือแค่ที่ยังไม่ปิดงาน) ผมตั้งให้ sync **เฉพาะออเดอร์ที่ยังไม่ปิด (`closed === false`)** เท่านั้น เพราะดึงทั้งหมดทุกรอบไม่คุ้ม/ไม่จำเป็น — ถ้าอยากให้ค้นหาออเดอร์เก่าที่ปิดแล้วได้ด้วย ต้องบอกผมเพิ่ม จะไปแก้เงื่อนไขในสคริปต์ให้

### 5. เก็บกวาดไฟล์ซ้ำเรียบร้อยแล้ว
ลบ `prewithmarry-app/scripts` และ `prewithmarry-app/.github` (สำเนาเก่าที่เขียนไว้ก่อนจะย้ายมาอยู่ที่ repo `shipnityreader` นี้) ออกแล้ว ไม่มีไฟล์ตกค้าง — ของจริงทั้งหมดอยู่ที่ repo นี้ที่เดียว

## สิ่งที่อยากให้ตรวจทานเป็นพิเศษ
ทุกอย่างข้างต้น "ทำเสร็จ" หมายถึงเขียนโค้ด+ทดสอบเท่าที่ทำได้โดยไม่แตะรหัสผ่านจริงเท่านั้น **การรันจริงครั้งแรกยังไม่เกิดขึ้น** — อย่าเพิ่งมั่นใจว่าระบบอัตโนมัติทำงานจริงจนกว่าจะกด Run workflow ด้วยตัวเองแล้วเห็นผล "Synced N orders successfully" ในหน้า Actions log ก่อน
